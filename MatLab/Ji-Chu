- ans 是特殊变量, 用于存储最近一次的计算结果
      如果执行了一行代码, 但并没有指定输出参数时, MatLab会自动创建 ans 变量, 并将计算结果存储在其中
- disp 类似于C语言中的printf()函数, 输出
- input 输入
- MatLab 变量区分大小写

- MatLab矩阵有两种写法, 可以直接写到一行; 也可以写成多行 
两行之间以分号结尾, 最后一行的分号可加可不加, 同行之间的元素以空格或逗号分开

- 注释:
      单行注释用百分号 % 
      多行注释用 %{ %}
 快捷键注释:  Ctrl + R
   取消注释:  Ctrl + T
     

